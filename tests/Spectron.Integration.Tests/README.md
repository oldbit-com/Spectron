# Integration Tests

This project contains integration tests. They execute the actual emulator with test scenarios.

The tests are executed for x number of frames and results are compared with expected hash.
Typicall this is the hash of the screen memory buffer. Execution is always deterministic.